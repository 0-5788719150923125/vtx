version: '3.9'

services:
  vtx:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${GPU_MANUFACTURER:-nvidia}
              capabilities: [gpu]
              device_ids: ['${TRAIN_GPU_INDEX:-0}']